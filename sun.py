# -*- coding: utf-8 -*-
"""sun.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1QX7Adzy4MGInPE40FpglmqOOo35TgoZF
"""

import numpy as np
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import r2_score

data=pd.read_csv(r"data.info()")
data.drop(['sqft_above'],axis='columns',inplace=True)
conv_d=[1 if values==2014 else 0 for values in data.date]
data['date']=conv_d

data.info()

data

data.hist(figsize=(15,8))

y=data[['price']].values
x=data.drop(['id','price'],axis=1).values

x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.5)

reg=LinearRegression()
reg.fit(x_train,y_train)

y_predict=reg.predict(x_test)
print(y_predict)
print(y_test)

y_predict=reg.predict(x_test)
print(y_predict)
print(y_test)